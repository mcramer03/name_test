/*
 * Initialise the search and response objects
 */

$(document).ready(function() {

    var result = $("#results_container").resultContainer();

    var search = $("#search_container").searchContainer(result);
    
});